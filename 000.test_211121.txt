211121 study

- github 가입 및 기초 명령어 정리

- 003/index.html
    PHP로 변경완료, 링크 전부수정, 아이템 슬라이드 인디케이터 수정
    // 내일 종합포폴사이트 PHP로 고치기😄! 
       학원나가는날 질문=> js에서 ajax 코드 잘짜는 방법 있는지
                          sql/ where cate_no=X and item_no=X 왜?않되?
                               문자중에 특정 단어 있는 row찾기
                               ajax {}안에서 정보해서 바깥에서 쓰는법(왜 안되는지 모르겠다)
                          PHP되는 유료서버 어디걸로 사야하는지?

    // 경험치 : init i++를 i--로 바꿀때: append는 depend로, push는 unshift로, {{index}}잘 잡기
    // 문제해결 - 조건 자체가 실행되는지 console로 찍어서 직접 확인
               - 조건에 해당하는 변수랑 대상 console에 하나씩 찍기
               - HTML이랑 같이 보면서 대상의 경로나 갯수, 존재여부(init) 확인
               - index잘 잡히는지 확인
               - 각 행 계산해서 옆에 적어가면서, console에 해당 계산 찍고 일치하는지 보기
               - 동작하는 시간 확인, console에 숫자 찍기
               - 스코프 맞는 곳에 있는지 확인(for문 안에 있다던가)
               - for문 i안에 for문 i를 옮기지 않았는지 확인, 변수이름 확인 ex)list

    // 문자열 잘라서 배열로 만들 때, 조건else일 때도 배열에 넣어야 하단에서 length통일 <<

- 내일 할일: 종합포폴사이트 수정, 이력서 작성, 004와이어프레임, 부트스트랩 실습